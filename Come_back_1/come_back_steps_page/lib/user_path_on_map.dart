import 'package:flutter/material.dart';

class UserPathOnMap extends StatelessWidget {
  const UserPathOnMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "Map",
          style: TextStyle(
            fontWeight: FontWeight.bold,
            color: Colors.white,
            fontSize: 16,
          ),
        ),
      ),
      body: Column(
        
      ),
    );
  }
}
